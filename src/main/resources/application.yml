server:
  port: 3338
spring:
  datasource:
    name: Jenkins
    url: jdbc:mysql://dev.zhongcheng.tech:8088/NodeJenkins2?useUnicode=true&characterEncoding=utf-8 #url
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      max-lifetime: 30000

  #mybatis:
  #mapper-locations: classpath:mapper/*.xml
  #type-aliases-package: com.zhongcheng.jenkins.bean
mybatis-plus:
  configuration:
    # 是否开启自动驼峰命名规则（camel case）映射，即从经典数据库列名 A_COLUMN（下划线命名） 到经典 Java 属性名 aColumn（驼峰命名） 的类似映射
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

